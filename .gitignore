# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
.terraform.lock.hcl

# Sensitive data
variables.tfvars
terraform.tfvars

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Docker secrets
docker/arcane/.env
docker/npm/.env
docker/frigate/.env

# Git hooks (local, should not be committed)
.githooks/
docker/.env
docker/qbittorrent/.env

# Proxmox /root/.env with Telegram secrets (deployed manually via scp)
/root/.env

# Frigate rendered config (use config.yml.j2 template instead)
docker/frigate/config/config.yml

# Agent/IDE config (user-specific)
.agents/
.atl/
.claude/
.cline/
.codebuddy/
.continue/
.kiro/
.ocx/
.opencode/
skills-lock.json
